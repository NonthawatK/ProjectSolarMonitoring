# ✨ Solar Rooftop Temperature Monitoring & Logging System With LAN Communication

## 🔧 โปรเจกต์นี้ใช้อุปกรณ์ ESP32 Xiao C3 

## ⭐ ฟีเจอร์หลัก
- อ่านอุณหภูมิจาก HTU21D ผ่าน I2C  
- อ่านเวลา DS3231 ผ่าน I2C  
- ส่งข้อมูลผ่าน ESP-NOW  
- ตั้งค่าเวลาและ MAC ผ่าน Web Server 
- เปิด WiFi AP เมื่อกดปุ่ม  
- บันทึกค่าการตั้งเวลาและ MAC ลง NVS  
โปรเจกต์นี้เหมาะสำหรับระบบ IoT, ระบบส่งข้อมูลไร้สาย

---

## 📂 โครงสร้างระบบโดยย่อ
⭐ สรุปสั้นที่สุด
ESP32 อ่านอุณหภูมิและเวลา → ส่งผ่าน ESP-NOW → เข้าสู่โหมด Deep Sleep
พร้อมมี Web Setup ให้ตั้งเวลาและ MAC ครั้งแรก

⭐ การทำงาน
📌 1) อ่านข้อมูลจากเซ็นเซอร์ผ่าน I2C
✔ HTU21D เพื่ออ่านอุณหภูมิ
✔ DS3231 เพื่ออ่านวัน–เวลาแบบ RTC
โดย ESP32 จะอ่านข้อมูลแล้วเก็บไว้ในตัวแปรส่วนกลาง

📌 2) ตรวจสอบว่า DS3231 ถูกตั้งเวลาหรือยัง
เมื่อเปิดเครื่อง ESP32 จะตรวจใน NVS
ถ้ายังไม่มีการตั้งเวลา → เข้าสู่ โหมด Web Server อัตโนมัติ
ถ้ามีแล้ว → ทำงานปกติ (อ่านค่า + ส่งข้อมูล)

📌 3) Web Server (โหมดตั้งค่า)
เมื่อยังไม่เคยตั้งเวลา:ESP32 จะ
เปิด WiFi AP ที่ชื่อว่า DS3231_Setup รหัส 12345678
เชื่อมต่อ WiFi จากอุปกรณ์แล้วเปิด Browser ไปที่ http://192.168.4.1
เพื่อทำการตั้งค่าผู้ใช้สามารถตั้งค่า
วันที่ / เวลา ของ DS3231
MAC Address ของตัวรับ ESP-NOW
เมื่อกด Save → บันทึกค่าใน NVS และรีสตาร์ทเครื่อง

📌 4) ESP-NOW Sender Mode
เมื่อผ่านการตั้งค่าแล้ว ESP32 จะทำงานดังนี้:
อ่านอุณหภูมิจาก HTU21D
อ่านเวลา DS3231
แปลงข้อมูลเป็นสตริง
ส่งข้อมูลไปยัง ESP-NOW Receiver ที่ MAC ถูกบันทึกไว้
รอ callback เพื่อตรวจสอบว่าส่งสำเร็จหรือไม่

📌 5) ใช้ Deep Sleep เพื่อลดพลังงาน (อธิบายแบบง่ายมาก)
หลังจากส่งข้อมูลเสร็จ ESP32 เข้าสู่ Deep Sleep เป็นเวลา 60 นาที เพื่อประหยัดพลังงาน
แต่ถ้าพยายามส่งข้อมูลครบ 30 ครั้งแล้วยังส่งไม่ได้ ก็จะ เข้าสู่ Deep Sleep 60 นาทีเหมือนกัน เพื่อไม่ให้เครื่องทำงานวนซ้ำจนเปลืองแบต
การใช้ Deep Sleep แบบนี้ทำให้อุปกรณ์ประหยัดพลังงานมาก เหมาะกับงานที่ต้องติดตั้งนาน ๆ เช่น
วัดอุณหภูมิแผงโซล่าเซลล์, งานเกษตร, ระบบ outdoor ที่ไม่มีไฟฟ้า ใช้แบตเตอรี่

📌 6) เก็บข้อมูลสำคัญลง NVS
NVS ใช้เก็บสถานะว่าตั้งเวลาแล้วหรือยัง (configured = 1)
MAC ของตัวรับ ESP-NOW ป้องกันข้อมูลหายเมื่อรีบูต
---

